<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/app.css">
    <title>Teacher Card <%= teacher.fullName %> </title>
</head>

<body>

    <a class="button button--red" href="/logout">Logout</a>
    <h1 class="main-header">Teacher Card <%= teacher.fullName %></h1>


    <table border="1">
        <thead>
            <th>No</th>
            <th>Student Name</th>
            <th>Date</th>
            <th>Status</th>
            <th>Action</th>
        </thead>

        <tbody>
            <% userTeachers.forEach((el,idx) => { %>
            <tr>
                <td><%= idx+1 %> </td>
                <td><%= el.User.username %> </td>
                <td><%= formatDate(el.date) %> </td>
                <td><%= el.status %> </td>
                <td>

                    <% if (el.status === 'pending') { %>
                    <a class="button" href="/approve/<%= el.id %> ">Approve</a>
                    <a class="button" href="/reject/<%= el.id %>">Reject</a>
                    <% } else { %>
                    <p>No action needed</p>
                    <% } %>


                </td>
            </tr>
            <% }) %>
        </tbody>
    </table>


</body>

</html>